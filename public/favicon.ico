<!-- This would be a binary ICO file, but since I can't create binary files, 
     the SVG version will work for modern browsers. 
     For production, you should convert the SVG to ICO format using an online converter -->